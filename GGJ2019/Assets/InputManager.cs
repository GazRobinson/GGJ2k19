using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InputManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
      /*  for ( int i = 0; i < 10; i++ ) {
            if ( Input.GetButtonDown( i.ToString() )){
                print( i );
            }

        }*/
      /*  Vector2 inputDir = new Vector2( Input.GetAxis( "HorizontalA" ), Input.GetAxis( "VerticalA" ) );
        if ( inputDir.sqrMagnitude > 0 ) {
            Debug.Log( "A: " + inputDir );
        }

        Vector2 inputDirB = new Vector2( Input.GetAxis( "HorizontalB" ), Input.GetAxis( "VerticalB" ) );

        if ( inputDirB.sqrMagnitude > 0 ) {
            Debug.Log( "B: " + inputDirB );
        }

        for ( int i = 0; i < 20; i++ ) {
            if ( Input.GetKeyDown( "joystick 1 button " + i ) ) {
                print( "joystick 1 button " + i );
            }
        }*/
    }
}
